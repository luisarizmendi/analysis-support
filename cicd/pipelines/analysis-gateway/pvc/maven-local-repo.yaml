apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: analysis-gateway-maven-local-repo
spec:
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain 